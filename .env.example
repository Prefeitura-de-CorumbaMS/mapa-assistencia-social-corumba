# ============================================================================
# MAPA DA ASSISTÊNCIA SOCIAL - CORUMBÁ/MS
# Arquivo de exemplo de variáveis de ambiente
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Conexão com MySQL
# Formato: mysql://usuario:senha@host:porta/nome_do_banco
DATABASE_URL="mysql://root:senha@localhost:3306/mapa_assistencia"

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Secret para assinar tokens JWT (use uma string aleatória e segura)
# Gere uma chave segura com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="seu-secret-jwt-aqui-substitua-por-string-aleatoria-segura"

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# Ambiente de execução (development, production)
NODE_ENV="development"

# Porta da API
PORT=8011

# ============================================================================
# FRONTEND CONFIGURATION (Vite)
# ============================================================================
# Porta do frontend (configurada no vite.config.js)
# VITE_PORT=8012

# URL da API (se diferente de localhost)
# VITE_API_URL="http://localhost:8011"

# ============================================================================
# OBSERVAÇÕES
# ============================================================================
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Substitua os valores de exemplo pelos valores reais
# 3. NUNCA commite o arquivo .env no git (ele está no .gitignore)
# 4. Em produção, use variáveis de ambiente do sistema ou secrets do hosting
